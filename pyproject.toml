[project]
name = "genai-arch-reviewer"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "boto3>=1.34.0",
  "fastapi>=0.111.0",
  "uvicorn>=0.29.0",
]

[tool.vercel]
entrypoint = "api.index:app"
